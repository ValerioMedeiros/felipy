# First steps to run the web editor 

This project has a requirement [Node.Js](https://nodejs.org) that must be 
installed before. Now, open a terminal and type the following commands:
``` 
$npm install 
$npm run dev
``` 


